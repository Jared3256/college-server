Read `AGENTS.md` at the server/AGENTS.md of the project before starting 

do the following:
1. update the create-student-dto.ts to have data that matches those in student entity
2. modify the create endpoint to enable students be able to register. in the student service ensure that all critical models are created first before registering the student and saving to the database. create any supporting models that are linked to the student as well.
3. modify the other endpoints in the student controller to ensure the student completes the CRUD operations both in the controller and the service layer

## Complete when 
1. student can successfully register to the system  and all the relevant model are created to complete the relations
2. student can successfully complete the crud operations